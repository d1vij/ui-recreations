// automatically prerender static pages
export const prerender = "auto";

// enable ssr so that prerendering works during build time
export const ssr = true;

// setting up 404.html as fallback for github pages enables us to
// use client side rendering for  "non prerendered" pages
